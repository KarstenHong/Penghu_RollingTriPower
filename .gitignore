scratch/
.vscode/
.DS_Store
Thumbs.db

# Firebase CLI 設定/規則檔 — 不應該被 GitHub Pages 當成網站內容公開發布
# （GitHub Pages 直接把 main 分支根目錄整包當網站，這些檔案本來只是給
# `firebase deploy --only firestore:rules` 用，留在本機即可，不需要進 repo）
.firebaserc
firebase.json
firestore.rules

# 內部文件與交接資料 — 同樣不進版控（GitHub Pages 會把 repo 根目錄整包公開，
# 這些是給接手的人／AI 讀的內部說明，不是網站內容）
技術規格.md
使用者說明.md
*.pptx

# 本機開發工具 — 同樣不進版控（GitHub Pages 會把根目錄整包公開，這些不是網站內容）
# serve.js            本地預覽伺服器（node serve.js）
# _audit-overrides.js 檢查 animated.css 有沒有無條件蓋掉 style.css 的響應式設定
serve.js
_audit-overrides.js
